// libs
import { React } from '~/src/libs/packages'

// prettier-ignore
export const Component: React.FCC = ({ children }) => {
    return (
        <header>
            {children}
        </header>
    )
}
