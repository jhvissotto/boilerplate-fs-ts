import { initialize } from '.'

export const { v0, v1, v2, v3, v4 } = initialize({
  v0: {
    // captureRejections
  },
  v1: {
    // captureRejections
  },
  v2: {
    // delimiter,
    // ignoreErrors,
    // maxListeners,
    // newListener,
    // removeListener,
    // verboseMemoryLeak,
    // wildcard,
  },
  // v3: {},
  v4: {
    // debug
  },
})
